// [hanbao] Coding Mode removed; Monaco editor is no longer bundled.
// This file is kept as a placeholder so `tsc -b` (which compiles every file
// under src/) still passes. It no longer imports monaco-editor.
export {};
