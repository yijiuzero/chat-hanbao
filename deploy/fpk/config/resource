# ============================================================
# 飞牛 FPK 资源配置（config/resource）
#
# ⚠️ 待飞牛官方 FPK 规范核对：文件名/字段名为基于 2026-08-19 调研的
# 最佳实践猜测。hanbao 为 Python(uv) + React 应用，镜像约 1.78GB，
# 运行时需一定内存供 uvicorn + 前端服务。以下数值为建议下限，发布前按
# 飞牛 resource schema 校正（也可能为 docker-project 形态的 resource 声明）。
# ============================================================
cpu: 2
memory: 2048
disk: 20480
