#!/bin/sh
# ============================================================
# hanbao FPK 配置回调（config_callback）
#
# 飞牛在用户于应用设置页修改环境变量"后"调用本脚本。本项目的认证凭据在
# 安装时注入（install_callback），运行期配置变更需重启容器方可生效，此处
# 默认成功，由飞牛重启机制承接。
# ============================================================
exit 0
