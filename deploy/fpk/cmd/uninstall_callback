#!/bin/sh
# ============================================================
# hanbao FPK 卸载回调（uninstall_callback）
#
# 飞牛在"卸载后"调用本脚本。容器与镜像由系统处理，此处仅做日志提示。
# 不删除 TRIM_PKGVAR / TRIM_PKGETC 中的用户数据（交由飞牛卸载策略）。
# ============================================================
set -eu
exit 0
