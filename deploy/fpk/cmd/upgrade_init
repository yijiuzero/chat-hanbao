#!/bin/sh
# ============================================================
# hanbao FPK 升级初始化（upgrade_init）
#
# 飞牛在"升级前"调用本脚本。docker-project 形态下应用文件（app/）由系统整体
# 覆盖，数据卷（TRIM_PKGVAR）与持久配置（TRIM_PKGETC）不受影响，无需额外操作。
# 镜像已在安装时 load 进本地，compose pull_policy:never 不拉外网，升级沿用既存镜像。
# ============================================================
set -eu
exit 0
