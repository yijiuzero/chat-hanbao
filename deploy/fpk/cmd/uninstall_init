#!/bin/sh
# ============================================================
# hanbao FPK 卸载初始化（uninstall_init）
#
# 飞牛在"卸载前"调用本脚本。docker-project 形态下容器由系统停止/移除，
# 无需手动操作。用户数据（TRIM_PKGVAR）与持久配置（TRIM_PKGETC）的保留或
# 清理由飞牛卸载流程决定，此处不删除用户数据。
# ============================================================
set -eu
exit 0
