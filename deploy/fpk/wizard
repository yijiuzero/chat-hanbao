# ============================================================
# 飞牛 FPK 安装向导定义（wizard）
#
# ⚠️ 待飞牛官方 FPK 规范核对：向导文件格式（文件名/结构/是否 JSON）为
# 基于 2026-08-19 调研的最佳实践猜测。核心规则已确认（WechatOnCloud/fnos 实测）：
#   「向导字段名 = 注入的环境变量名」，无 wizard_ 前缀。
# 例如字段 HANBAO_AUTH_USERNAME → 安装回调中以 ${HANBAO_AUTH_USERNAME} 传入，
# install_callback 再持久化到 hanbao.env。
#
# 字段用途：首启自动建管理员账号（接 I-007 默认开认证 + auto_register_from_env），
# 消除「未注册前跳过认证」的局域网抢注窗口。用户留空则跳过后走首启注册页。
# ============================================================

fields:
  - name: HANBAO_AUTH_USERNAME
    label: 管理员账号
    type: text
    required: false
    default: admin
    help: 留空则首次打开页面进入注册页自行设置

  - name: HANBAO_AUTH_PASSWORD
    label: 管理员密码
    type: password
    required: false
    help: 留空则首次打开页面进入注册页自行设置
